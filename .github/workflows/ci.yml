name: CI Backend

on:
  push:
    branches: [production]
  pull_request:
    branches: [main]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "10.0.x"

      - name: Restore dependencies
        run: dotnet restore AirlineTicket.Backend.slnx

      - name: Build
        run: dotnet build AirlineTicket.Backend.slnx --configuration Release --no-restore

      - name: Build SignalR
        run: dotnet build src/Realtime/AirlineTicket.SignalR/AirlineTicket.SignalR.csproj --configuration Release --no-restore

      - name: Test
        run: dotnet test AirlineTicket.Backend.slnx --configuration Release --no-build --verbosity normal --filter "FullyQualifiedName!~IntegrationTests"

  # deploy:
  #   needs: build-and-test
  #   if: github.ref == 'refs/heads/production' && github.event_name == 'push'
  #   runs-on: ubuntu-latest

  #   steps:
  #     - uses: actions/checkout@v4

  #     - name: Set up Docker Buildx
  #       uses: docker/setup-buildx-action@v3

  #     - name: Login to Docker Hub
  #       uses: docker/login-action@v3
  #       with:
  #         username: ${{ secrets.DOCKERHUB_USERNAME }}
  #         password: ${{ secrets.DOCKERHUB_TOKEN }}

  #     - name: Build and push Docker image
  #       uses: docker/build-push-action@v5
  #       with:
  #         context: .
  #         push: true
  #         tags: ${{ secrets.DOCKERHUB_USERNAME }}/airlineticket-backend:latest

  #     - name: Deploy to Production Server
  #       uses: appleboy/ssh-action@master
  #       with:
  #         host: ${{ secrets.PROD_SERVER_HOST }}
  #         username: ${{ secrets.PROD_SERVER_USER }}
  #         key: ${{ secrets.PROD_SERVER_SSH_KEY }}
  #         script: |
  #           cd /opt/airlineticket/backend
  #           docker pull ${{ secrets.DOCKERHUB_USERNAME }}/airlineticket-backend:latest
  #           docker-compose -f deploy/docker/docker-compose.prod.yml down
  #           docker-compose -f deploy/docker/docker-compose.prod.yml up -d
